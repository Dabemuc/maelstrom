Make graph nodes pure + stateless

Replace linear pipeline with DAG

Add parameter hashing for cache invalidation

Add tiling to LinearImage

Add multithreaded execution (Rayon)

Add basic file loading (PNG first, not RAW)

Only then start UI work